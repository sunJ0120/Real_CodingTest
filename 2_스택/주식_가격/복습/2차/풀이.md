## 1차 풀이

```java
import java.util.*;
import java.lang.*;

class Solution {
    public int[] solution(int[] prices) {
        int[] answer = new int[prices.length];
        
        for(int i = 0; i<prices.length; i++){
            int val = 0;
            for(int j = i+1; j<prices.length; j++){
                val++;
                if(prices[j] < prices[i]){
                    break;
                }
            }
            answer[i] = val;
        }
        
        return answer;
    }
}
```
- 솔직히 Stack 쓰는거 기억 안나서 이렇게 푼건데
- 아니 이거 O(N^2)인데 왜 효율성까지 통과하는건지 전혀 모르겠고
- 사실상 좋은 풀이는 아니다.

## 최종 풀이

- 이 문제의 핵심은, 길이를 정할 수 있는 것은 고정하고 들어간다는 것이다.
- 떨어지는 지점에서 이전 것들을 차례대로 보면서, 자기보다 큰 것이 있으면 수를 늘려서 길이를 고정하는 것이다.
  - 결국, 그 뒤에 있는 것은 그동안 오름차순이 잘 지켜진거라, 점점 커진다는게 보장되어 있으므로 순서대로 보면 된다.
- 그리고 끝까지 스택에 남아 있는 것은, 사실상 계속 증가가 보장된 것들이라 길이-1-그거 인덱스 해주면 된다.
- 이렇게 한 방향으로의 증가 감소를 유지해야 하는 경우, 스택의 **뽑는 특성**을 이용해서 검사하고 스택에 한 방향만 남도록 하고, 어긋나는걸 미리 고정하는 방식으로 하면 쉽게 풀 수 있다.

```java
import java.util.*;
import java.lang.*;

class Solution {
    public int[] solution(int[] prices) {
        int[] answer = new int[prices.length];
        // 1. 스택을 생성한다.
        Stack<Integer> stack = new Stack<Integer>();
        stack.push(0);
        // 2. top (올라간것) 보다 data (올라갈 것)가 더 작으면 1부터 길이 고정한다. (스택 계속 보기)
        for(int i = 1; i<prices.length; i++){
            int data = prices[i];
            while(!stack.isEmpty()){
                int topInd = stack.peek();
                if(prices[topInd] > data){
                    answer[topInd] = i-topInd;  // 이 부분 시간의 차이를 구하는 것이라는걸 명심하자.
                    stack.pop();
                }else{
                    break;
                }
            }
            // 스택에 인덱스 올린다.
            stack.push(i);
        }
        // 3. 끝까지 남은 것들은 길이 - 인덱스 - 1 해서 앞에서 남은 길이를 구해서 넣는다.
        while(!stack.isEmpty()){
            int ind = stack.pop();
            answer[ind] = prices.length - ind - 1;
        }
        return answer;
    }
}
```

- 중요한 것은, 저장 및 비교를 동시에 하기 위해 인덱스를 올린다는 것이다.
- 또한, 값을 저장할때는 data와 top의 거리를 계산한다는 것 (그래야 앞에 것 넣을때 얼마만큼 차이 나는지 넣을 수 있다.)

> 🚨 문제 생각 안나고, 어려워도 당황하지 말고 내가 알고 있는 선에서 어떤 개념을 쓰는 것과 가장 가까운가를 보도록 하자.
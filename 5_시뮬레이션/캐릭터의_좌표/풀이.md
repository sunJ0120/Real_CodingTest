## 풀이 
```java
import java.util.*;

class Solution {
    public static int xInd;
    public static int yInd;
    
    public void move(int moveX, int moveY, int[] answer){
        int nextX = answer[0] + moveX;
        int nextY = answer[1] + moveY;
        
        if(-xInd <= nextX && nextX <= xInd){
            if(-yInd <= nextY && nextY <= yInd){
                answer[0] = nextX;
                answer[1] = nextY;
            }
        }
    }
    
    public int[] solution(String[] keyinput, int[] board) {
        int[] answer = new int[2];
        
        xInd = board[0] / 2;
        yInd = board[1] / 2;
        
        for(String input : keyinput){
            if("left".equals(input)){
                move(-1, 0, answer);
            }else if("right".equals(input)){
                move(1, 0, answer);
            }else if("up".equals(input)){
                move(0, 1, answer);
            }else{
                move(0, -1, answer);
            }
        }
        
        return answer;
    }
}
```
- 이렇게 풀었는데, 사실상 저 인덱스 bounds 체크 같은 경우는 절댓값으로 비교하면 더 쉽다.

## 최종 풀이

```java
import java.lang.*;

class Solution {
    public static int xInd;
    public static int yInd;
    
    public void move(int moveX, int moveY, int[] answer){
        int nextX = answer[0] + moveX;
        int nextY = answer[1] + moveY;
        
        if(Math.abs(nextX) <= xInd &&  Math.abs(nextY) <= yInd){
            answer[0] = nextX;
            answer[1] = nextY;
        }
    }
    
    public int[] solution(String[] keyinput, int[] board) {
        int[] answer = new int[2];
        
        xInd = board[0] / 2;
        yInd = board[1] / 2;
        
        for(String input : keyinput){
            if("left".equals(input)){
                move(-1, 0, answer);
            }else if("right".equals(input)){
                move(1, 0, answer);
            }else if("up".equals(input)){
                move(0, 1, answer);
            }else{
                move(0, -1, answer);
            }
        }
        
        return answer;
    }
}
```

```
>> 정확성  테스트
테스트 1 〉	통과 (0.04ms, 83MB)
테스트 2 〉	통과 (0.01ms, 74.6MB)
테스트 3 〉	통과 (0.04ms, 84.1MB)
테스트 4 〉	통과 (0.05ms, 88.6MB)
테스트 5 〉	통과 (0.03ms, 84.7MB)
테스트 6 〉	통과 (0.03ms, 84MB)
테스트 7 〉	통과 (0.05ms, 82.2MB)
테스트 8 〉	통과 (0.04ms, 73.9MB)
테스트 9 〉	통과 (0.04ms, 79.6MB)
테스트 10 〉	통과 (0.03ms, 87.3MB)
테스트 11 〉	통과 (0.04ms, 87.7MB)

>> 채점 결과
정확성: 100.0
합계: 100.0 / 100.0
```